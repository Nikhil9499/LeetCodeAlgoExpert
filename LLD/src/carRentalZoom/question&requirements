- Build a car rental system like zoomCar

- Flow:
    - A contestant comes on the application and enter the location from where the contestant needs to book the car.
    - Based on location selected, application will show list of stores from where the contestant can select a car.
    - After selecting the store, the contestant will see the list of cars available for rent.
    - The contestant will select a car and proceed ahead with booking.
    - During booking, the contestant needs to mention the journey details like time duration, startTime, type of booking(hourly/daily/etc).
    - After everything is done, a bill will be generated that the contestant can pay before/after the journey completes.

